using UnityEngine;

public class Cat_cat : MonoBehaviour
{
    // ねこ
    [SerializeField] GameObject american;  // アメショー
    [SerializeField] GameObject bengal;    // ベンガル
    [SerializeField] GameObject munchkin;  // マンチカン
    [SerializeField] GameObject norwegian; // ノルウェージャン
    [SerializeField] GameObject ragdool;   // ラグドール
    [SerializeField] GameObject scottish;  // スコティッシュ
    [SerializeField] GameObject black;     // 黒ネコ
    [SerializeField] GameObject white;     // 白ネコ
    [SerializeField] GameObject sabatora;      // サバトラ
    [SerializeField] GameObject cyatora;   // 茶トラ

    [SerializeField] GameObject[] Cat = new GameObject[14];

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
